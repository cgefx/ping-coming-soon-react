import styled from 'styled-components'

export const StyledSection = styled.section`
  margin: 0 auto;
  padding: 4.8rem 2.4rem;
  max-width: 120rem;

  :not(:first-of-type) {
    padding-top: 0;
  }
`
