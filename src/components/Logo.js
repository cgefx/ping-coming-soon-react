import React from 'react'
import LogoImage from '../images/logo.svg'

const Logo = () => {
  return (
    <header>
      <img src={LogoImage} alt="Ping Logo" />
    </header>
  )
}

export default Logo
